import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Title Verification · Pre-Purchase Property Search',
  description: 'B2B title verification for rural property. ₹500–₹5,000 per check. Used by banks, NBFCs, and buyers across India.'
};
export default function L({ children }: { children: React.ReactNode }) { return children; }
