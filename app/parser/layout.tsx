import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Gharauni Parser API · Developer API for SVAMITVA cards',
  description: 'REST API to extract structured data from Gharauni / SVAMITVA card PDFs. ₹2/parse at scale. Free tier 100/month.'
};
export default function L({ children }: { children: React.ReactNode }) { return children; }
